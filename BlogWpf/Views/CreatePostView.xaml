<Window x:Class="BlogWpf.Views.CreatePostView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlogWpf.Views"
        mc:Ignorable="d"
        Title="CreatePostView" Height="400" Width="450"
        Background="#F1F8F4 ">
    <Window.Resources>
        <Style x:Key="CreateButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4F8F6B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
        <StackPanel
            Orientation="Vertical"
            Grid.Row="0">
            <TextBlock Text="Create New Post" 
                       FontSize="30" 
                       FontWeight="Bold" 
                       Foreground="#2D3436"
                       HorizontalAlignment="Left"/>
            <TextBlock Text="Title" 
                       FontSize="15" 
                       FontWeight="Bold" 
                       Foreground="#2D3436"
                       HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Title}" FontSize="15" Margin="0,0,0,10"/>
            <TextBlock Text="Content" 
                       FontSize="15" 
                       FontWeight="Bold" 
                       Foreground="#2D3436"
                       HorizontalAlignment="Left"/> 
            <TextBox Text="{Binding Content}" FontSize="15" Margin="0,0,0,10"/>
            <Button Content="+ Create Post" 
                    Style="{StaticResource CreateButtonStyle}" 
                    Command="{Binding CreatePostCommand}"
                    Width="150" 
                    HorizontalAlignment="Left"
                    Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
