<Window x:Class="BlogWpf.Views.CreatePostView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlogWpf.Views"
        mc:Ignorable="d"
        Title="CreatePostView" Height="400" Width="450">
    <StackPanel
        Orientation="Vertical">
        <Label Content="Create New Post" FontSize="30" HorizontalAlignment="Center" Margin="10"/>
        <TextBlock Text="Title:" Margin="10,0,10,5"/>
        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,10,10" Height="30"/>
        <TextBlock Text="Content:" Margin="10,0,10,5"/>
        <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,10,10" Height="100"/>
        <Button Content="Create" Margin="10" Height="30" Width="100" HorizontalAlignment="Center" Command="{Binding CreatePostCommand}" Click="Button_Click"/>
    </StackPanel>
</Window>
