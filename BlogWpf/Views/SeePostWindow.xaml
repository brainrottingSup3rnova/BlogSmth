<Window x:Class="BlogWpf.Views.SeePostWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlogWpf"
        mc:Ignorable="d"
        Title="SeePostWindow" Height="460" Width="460"
        Background="#F1F8F4">
    <Window.Resources>
        <Style x:Key="ButtonDeleteStyle" TargetType="Button">
            <Setter Property="Background" Value="#C62828"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                            CornerRadius="8" 
                            BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ButtonUpdateStyle" TargetType="Button">
            <Setter Property="Background" Value="#7FBF9E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                            CornerRadius="8" 
                            BorderThickness="0">    
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel
            Orientation="Vertical">
            <TextBlock
                Text="See post"
                FontWeight="Bold"
                FontSize="30"
                HorizontalAlignment="Left"/>
            <TextBlock
                Text="Title"
                FontWeight="Bold"
                FontSize="15"
                HorizontalAlignment="Left"/>
            <TextBox
                Text="{Binding EditedTitle}"/>
            <TextBlock
                Text="Content"
                FontWeight="Bold"
                FontSize="15"
                HorizontalAlignment="Left"/>
            <TextBox
                Text="{Binding EditedContent}"
                MaxHeight="200"
                TextWrapping="Wrap"/>
            <Button
                Content="Update"
                Style="{StaticResource ButtonUpdateStyle}"
                Command="{Binding UpdatePostCommand}"
                Click="Save_Click"/>
            <Button
                Content="Delete"
                Style="{StaticResource ButtonDeleteStyle}"
                Command="{Binding DeletePostCommand}"
                Click="Delete_Click"/>
        </StackPanel>
    </Grid>
</Window>
